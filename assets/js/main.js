/**
 * Main JavaScript
 * Aakaari Brand Theme
 */

(function($) {
    'use strict';

    // Document ready
    $(document).ready(function() {
        console.log('Aakaari Brand Theme Loaded');

        // Add your custom JavaScript here
    });

    // Window load
    $(window).on('load', function() {
        // Code to run after page is fully loaded
    });

    // Window scroll
    $(window).on('scroll', function() {
        // Code to run on scroll
    });

})(jQuery);
